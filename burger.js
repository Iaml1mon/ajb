const BURGER_IMG='https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&fit=crop&w=1200&q=90';
(async()=>{const detail=document.querySelector('#detail');const burgers=await fetch('burgers.json').then(r=>r.json());const id=new URLSearchParams(location.search).get('id');const b=burgers.find(x=>x.id===id)||burgers[0];const esc=s=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));document.title=`${b.name} — AJ Burger`;detail.innerHTML=`<div class="product-gallery"><div class="detail-photo"><img src="${BURGER_IMG}" alt="${esc(b.name)}"></div><div class="thumbs">${[1,2,3,4].map((n,i)=>`<button type="button" class="thumb ${i===0?'active':''}" aria-label="Burger photo ${n}"><img src="${BURGER_IMG}" alt=""></button>`).join('')}</div></div><article class="detail"><div class="detail-label">☆ ${esc(b.badge)}</div><h1>${esc(b.name)}</h1><div class="detail-price">$${b.price}</div><p class="detail-desc">${esc(b.desc)}</p><div class="ingredients"><h3>What's inside</h3>${b.ingredients.map(i=>`<div class="ingredient">${esc(i)}</div>`).join('')}</div><a class="detail-cta" href="index.html">← Back to Burgers</a></article>`;document.querySelectorAll('.thumb').forEach(t=>t.addEventListener('click',()=>{document.querySelectorAll('.thumb').forEach(x=>x.classList.remove('active'));t.classList.add('active')}));})().catch(()=>document.querySelector('#detail').innerHTML='<div class="empty">Burger could not load.</div>');